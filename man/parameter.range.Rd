% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parameter.range.R
\name{parameter.range}
\alias{parameter.range}
\title{Calculate Parameter Ranges}
\usage{
parameter.range(
  raw.data.param.alpha,
  raw.data.param.beta,
  raw.data.param.sigma,
  option = "mean.sd"
)
}
\arguments{
\item{raw.data.param.alpha}{Numeric vector or data for the parameter alpha.}

\item{raw.data.param.beta}{Numeric vector or data for the parameter beta.}

\item{raw.data.param.sigma}{Numeric vector or data for the parameter sigma.}

\item{option}{Character. Either \code{"mean.sd"} to return the mean and standard deviation, or \code{"min.max"} to return the minimum and maximum values. Default is \code{"mean.sd"}.}
}
\value{
A list containing the processed values for:
\itemize{
\item \code{alpha}: Processed values for the alpha parameter.
\item \code{beta}: Processed values for the beta parameter.
\item \code{sigma}: Processed values for the sigma parameter.
}
}
\description{
This function processes the input data for parameters alpha, beta, and sigma, and returns either the mean and standard deviation or the minimum and maximum values, based on the specified option.
}
\details{
This function takes in raw data for parameters alpha, beta, and sigma. Depending on the \code{option} argument, it calculates either:
\itemize{
\item the minimum and maximum values (\code{min.max}), or
\item the mean and standard deviation (\code{mean.sd}).
The function uses helper functions \code{get.min.max()} and \code{get.mean.sd()} to perform these calculations.
If an invalid option is provided, the function will stop and return an error message.
}
}
\examples{
# Example with mean and standard deviation option
alpha <- rnorm(100)
beta <- rnorm(100)
sigma <- rnorm(100)
param_ranges <- parameter.range(alpha, beta, sigma, option = "mean.sd")
print(param_ranges)

# Example with min and max option
param_ranges_min_max <- parameter.range(alpha, beta, sigma, option = "min.max")
print(param_ranges_min_max)

}
