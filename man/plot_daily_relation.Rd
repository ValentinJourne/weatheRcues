% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_daily_relation.R
\name{plot_daily_relation}
\alias{plot_daily_relation}
\title{Plot Daily Weather-Seed Relationships}
\usage{
plot_daily_relation(
  daily.relation.res,
  calendar,
  x_axis = c("reversed.day", "doy"),
  whattoplot = c("estimate", "correlation", "p.value", "r.squared"),
  binary_colors = c("#00AFBB", "#FC4E07"),
  highlight_window = NULL,
  yseg = 0.2
)
}
\arguments{
\item{daily.relation.res}{A data frame with daily results (e.g., from \code{runing_daily_relationship()}) that includes
columns such as \code{days.reversed}, \code{correlation}, \code{estimate}, \code{p.value}, or \code{r.squared}.}

\item{calendar}{A calendar data frame generated by \code{generate_reverse_day_calendar()} that includes \code{days.reversed}
and \code{DOY} (day-of-year) for plotting options.}

\item{x_axis}{Character string specifying which x-axis to use. Either \code{"reversed.day"} (default) or \code{"doy"}.}

\item{whattoplot}{Character string indicating which metric to visualize. Options: \code{"estimate"}, \code{"correlation"}
(default), \code{"p.value"}, or \code{"r.squared"}.}

\item{binary_colors}{A character vector of two colors to represent positive and negative values. Default:
\code{c("#00AFBB", "#FC4E07")}.}

\item{highlight_window}{Optional numeric vector of one or two day values (e.g., \code{c(400, 460)}) to mark with vertical
dashed lines and optionally a horizontal bar segment across the specified period.}

\item{yseg}{Numeric value for the y-position of the horizontal segment used to annotate \code{highlight_window} (default: 0.2).}
}
\value{
A \code{ggplot2} object showing the daily relationship between a weather variable and seed production, with
bars colored by sign (positive/negative) or uniformly for other metrics.
}
\description{
This function generates a bar plot visualizing the relationship between daily weather variables and seed production
metrics (e.g., slope estimates, correlation, p-values, R²) across reversed time (days before an event) or by
calendar day-of-year (DOY). It supports binary coloring for positive/negative effects or uniform color when plotting
p-values and R².
}
\examples{
\dontrun{
calendar <- generate_reverse_day_calendar(refday = 305, lastdays = 180, yearback = 1)
plot_daily_relation(
  daily.relation.res = daily_output,
  calendar = calendar,
  x_axis = "reversed.day",
  whattoplot = "correlation",
  binary_colors = c("steelblue", "firebrick"),
  highlight_window = c(398, 458),
  yseg = 0.15
)
}

}
