% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/daily_parameters_rolling_climate.R
\name{daily_parameters_rolling_climate}
\alias{daily_parameters_rolling_climate}
\title{Run a daily relationship between weather and biological variable}
\usage{
daily_parameters_rolling_climate(
  bio_data = bio_data,
  rolling.data = rolling.data,
  method = "spearman",
  formula_model = formula("log.seed~temperature"),
  model_type = "lm"
)
}
\arguments{
\item{bio_data}{A data frame containing biological data, including at minimum the columns \code{year}, \code{log.seed}, \code{sitenewname}, and \code{plotname.lon.lat}.}

\item{rolling.data}{A data frame containing rolling climate data, including \code{days.reversed}, \code{year}, and the rolling climate covariate used in modeling.}

\item{method}{Character. Correlation method used to estimate correlation coefficients. Options: \code{"spearman"} (default), \code{"pearson"}, or \code{"kendall"}.}

\item{formula_model}{A formula indicating the response and predictor (e.g., \code{log.seed ~ TMEAN_rolling}). The response must exist in \code{bio_data}, and the predictor in \code{rolling.data}.}

\item{model_type}{Character. Type of model to fit. Either \code{"lm"} (default) for linear regression or \code{"betareg"} for beta regression. The latter assumes the response is bounded between 0 and 1.}
}
\value{
A data frame containing:
\itemize{
\item \code{sitenewname}, \code{plotname.lon.lat}, \code{days.reversed}
\item Model estimates: \code{estimate}, \code{std.error}, \code{p.value}
\item Performance metrics: \code{r.squared}, \code{logLik}
\item Correlation results: \code{correlation}, \code{pvalue.cor}, \code{correlation.se}
}
}
\description{
This function performs a day-by-day analysis of the relationship between a rolling climate variable and a biological response (e.g., seed production). For each day (reversed day format), it fits a regression model and computes correlation metrics to identify lagged climate cues.
}
\details{
The function:
\itemize{
\item Merges \code{bio_data} and \code{rolling.data} by year.
\item Fits a linear or beta regression model for each unique value of \code{days.reversed}.
\item Extracts model slope, standard error, p-value, R², and log-likelihood.
\item Computes correlation and correlation standard error for each time point.
}

This function is intended to help detect temporal weather cues influencing biological processes across time.
}
\examples{
\dontrun{
result <- daily_parameters_rolling_climate(
  bio_data = my_bio_df,
  rolling.data = my_climate_rolling,
  method = "spearman",
  formula_model = log.seed ~ TMEAN_rolling
)
}

}
\seealso{
\code{\link{runing_daily_relationship}}, \code{\link{cor.test}}, \code{\link[betareg]{betareg}}, \code{\link[broom]{tidy}}
}
